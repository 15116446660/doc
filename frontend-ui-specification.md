# AI标书管理平台前端UI规范

## 一、通用规范

### 1.1 布局规范
- 页面采用响应式设计，支持桌面端和平板端
- 主体内容区域宽度：可根据屏幕自适应
- 左侧导航宽度：240px（可折叠）
- 顶部导航高度：64px
- 内容区域内边距：24px

### 1.2 颜色规范
- 主色调：#1890ff（品牌蓝）
- 辅助色：
  - 成功：#52c41a
  - 警告：#faad14
  - 错误：#f5222d
  - 链接：#1890ff
- 中性色：
  - 标题文字：#262626
  - 主要文字：#595959
  - 次要文字：#8c8c8c
  - 边框：#d9d9d9
  - 分割线：#f0f0f0
  - 背景：#f5f5f5

### 1.3 字体规范
- 主要字体：-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei'
- 字号：
  - 标题：20px
  - 副标题：16px
  - 正文：14px
  - 辅助文字：12px
- 行高：1.5

### 1.4 间距规范
- 组件间距：16px
- 区块间距：24px
- 表单项间距：24px
- 按钮间距：8px

## 二、列表页面规范

### 2.1 标书项目列表页

#### 过滤条件区域
- 位置：内容区顶部
- 布局：自适应网格布局
- 过滤条件包括：
  - 项目状态（Select）：全部、进行中、已完成、已废标
  - 项目类型（Select）：产品类、服务类、工程类
  - 时间范围（RangePicker）：创建时间、截止时间
  - 金额范围（InputNumber）：最小金额-最大金额
  - 关键词搜索（Input）：项目名称、编号、客户名称
  - 负责人（Select）：支持多选
  - 所属部门（TreeSelect）：部门层级选择

#### 视图切换
- 位置：过滤条件下方
- 功能：
  - 表格视图/卡片视图切换按钮
  - 批量操作按钮
  - 排序选择（按创建时间、截止时间、金额等）
  - 刷新按钮
  - 新建项目按钮

#### 表格视图
- 列配置：
  - 项目编号（可排序）
  - 项目名称（带链接，点击进入详情）
  - 客户名称
  - 项目类型
  - 项目金额（可排序）
  - 创建时间（可排序）
  - 截止时间（可排序）
  - 负责人
  - 当前状态（带状态标签）
  - 操作列
- 功能：
  - 支持列宽调整
  - 支持列显示隐藏配置
  - 支持列固定
  - 支持行选择
  - 支持数据导出

#### 卡片视图
- 卡片尺寸：300px x 360px
- 卡片内容：
  - 项目名称（带链接）
  - 项目状态标签
  - 关键信息展示：
    - 客户名称
    - 项目金额
    - 截止时间
    - 负责人头像
  - 进度条
  - 快捷操作按钮

#### 分页组件
- 位置：列表底部
- 功能：
  - 页码导航
  - 每页条数选择（10/20/50/100）
  - 总条数显示
  - 跳转页码输入

### 2.2 标书文档列表页

#### 过滤条件
- 文档类型（Select）：招标文件、投标文件、技术方案等
- 文档状态（Select）：草稿、审核中、已定稿等
- 创建时间范围（RangePicker）
- 关键词搜索（Input）：文档名称、内容
- 标签筛选（Tag）：支持多选

#### 文档列表
- 列表项展示：
  - 文档图标
  - 文档名称
  - 文档类型
  - 创建时间
  - 最后修改时间
  - 修改人
  - 文档大小
  - 状态标签
  - 操作按钮组

#### 批量操作
- 支持功能：
  - 批量删除
  - 批量移动
  - 批量标签设置
  - 批量导出

## 三、表单页面规范

### 3.1 标书创建/编辑表单

#### 基本信息表单
- 布局：两列表单布局
- 表单项：
  - 项目名称（Input）：必填，最大长度100
  - 项目编号（Input）：必填，自动生成，可修改
  - 项目类型（Select）：必填
  - 客户名称（AutoComplete）：必填，支持选择历史客户
  - 项目金额（InputNumber）：必填，支持精确到分
  - 投标截止时间（DateTimePicker）：必填
  - 开标时间（DateTimePicker）：必填
  - 保证金金额（InputNumber）：选填
  - 保证金截止时间（DateTimePicker）：选填
  - 项目描述（TextArea）：选填，支持富文本编辑
  - 项目标签（Select）：选填，支持多选和创建

#### 团队成员配置
- 布局：表格形式
- 字段：
  - 角色名称
  - 成员选择（Select）：支持多选
  - 权限配置
  - 任务分配
  - 操作

#### 文档模板选择
- 布局：卡片网格
- 显示内容：
  - 模板缩略图
  - 模板名称
  - 适用场景
  - 使用次数
  - 评分
- 支持预览和选择

### 3.2 表单验证规则
- 必填项校验
- 格式校验：
  - 金额：正数，最多两位小数
  - 时间：不能选择过去时间
  - 文本长度限制
- 关联验证：
  - 开标时间必须晚于投标截止时间
  - 保证金截止时间必须早于投标截止时间

## 四、详情页面规范

### 4.1 项目详情页

#### 页面布局
- 顶部：项目基本信息概览
- 左侧：导航菜单
- 主体：内容展示区
- 右侧：操作面板（可折叠）

#### 基本信息概览
- 布局：卡片式
- 内容：
  - 项目名称
  - 项目状态
  - 关键时间节点
  - 金额信息
  - 负责人信息
  - 快捷操作按钮

#### 导航菜单
- 项目概况
- 文档管理
- 团队成员
- 任务管理
- 时间节点
- 风险控制
- 审批记录
- 操作日志

#### 内容展示区
根据不同标签页显示对应内容，每个标签页都有独立的布局规范

#### 操作面板
- 固定在右侧
- 显示当前任务
- 显示待办事项
- 显示风险提醒
- 显示团队动态

### 4.2 文档详情页

#### 页面布局
- 顶部：文档信息栏
- 左侧：文档大纲
- 主体：文档编辑器
- 右侧：审阅评论面板

#### 文档信息栏
- 文档标题
- 当前状态
- 版本信息
- 协作者列表
- 操作按钮组：
  - 保存
  - 预览
  - 导出
  - 分享
  - 更多操作

#### 文档大纲
- 支持层级展示
- 支持拖拽排序
- 支持快速定位
- 支持编辑和删除

#### 文档编辑器
- 工具栏功能完整
- 支持实时协作
- 支持格式刷
- 支持插入表格、图片
- 支持页面缩放
- 支持全屏编辑

#### 审阅评论面板
- 显示所有评论
- 支持按人员筛选
- 支持按时间排序
- 支持回复和标记解决

## 五、交互规范

### 5.1 按钮规范
- 主要按钮：蓝色背景
- 次要按钮：白色背景，蓝色边框
- 危险按钮：红色背景或边框
- 禁用状态：灰色背景，不可点击
- 加载状态：显示加载动画
- 按钮尺寸：
  - 大：高度40px
  - 中：高度32px
  - 小：高度24px

### 5.2 表单交互
- 即时校验：输入时进行格式校验
- 提交校验：提交时进行完整性校验
- 错误提示：在表单项下方显示错误信息
- 帮助信息：悬浮显示帮助文本
- 联动效果：根据选择自动显示/隐藏相关字段

### 5.3 列表交互
- 点击行：显示详情或进入编辑
- 悬浮行：显示操作按钮
- 选择行：支持多选操作
- 拖拽排序：支持拖拽调整顺序
- 右键菜单：显示更多操作选项

### 5.4 弹窗规范
- 模态框尺寸：
  - 小：520px
  - 中：720px
  - 大：1024px
- 位置：居中显示
- 遮罩：半透明黑色
- 关闭方式：
  - 点击关闭按钮
  - 点击遮罩
  - ESC键

### 5.5 消息提示
- 成功提示：绿色，自动消失
- 警告提示：黄色，需要手动关闭
- 错误提示：红色，需要手动关闭
- 确认提示：模态框形式
- 加载提示：加载动画

### 5.6 页面切换
- 平滑过渡动画
- 加载状态显示
- 保留用户操作状态
- 支持快捷键导航

## 六、响应式设计

### 6.1 断点设定
- xs: <576px
- sm: ≥576px
- md: ≥768px
- lg: ≥992px
- xl: ≥1200px
- xxl: ≥1600px

### 6.2 响应式调整
- 导航栏：宽度小于768px时折叠为菜单按钮
- 表格：小屏幕下隐藏次要列，保留关键信息
- 表单：小屏幕下改为单列布局
- 卡片：根据屏幕宽度自动调整每行显示数量
- 字体：小屏幕下适当缩小字号

## 七、性能优化

### 7.1 列表优化
- 虚拟滚动：大量数据时采用虚拟滚动
- 分页加载：默认采用分页方式
- 数据缓存：缓存已加载的数据
- 按需加载：图片等资源按需加载

### 7.2 表单优化
- 分步表单：复杂表单采用分步形式
- 异步校验：远程校验采用防抖处理
- 自动保存：编辑状态下自动保存草稿
- 离线存储：支持离线编辑后同步

### 7.3 编辑器优化
- 分块加载：大文档采用分块加载
- 延迟渲染：复杂组件延迟渲染
- 协作优化：采用操作转换算法
- 定时同步：定期同步保存内容

## 八、辅助功能

### 8.1 快捷键支持
- 常用操作快捷键
- 导航快捷键
- 编辑器快捷键
- 快捷键提示

### 8.2 辅助提示
- 功能引导
- 上下文帮助
- 操作提示
- 错误恢复建议

### 8.3 无障碍支持
- 键盘导航
- 屏幕阅读器支持
- 高对比度模式
- 字体缩放 