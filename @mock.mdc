---
description: 
globs: 
alwaysApply: false
---
### 规则标题

**Mock 数据实现规范：统一使用 vite-plugin-mock，强制使用 Query Parameters**

### 规则描述
为了确保开发环境的统一性、可维护性，以及未来与真实后端接口的平滑对接，项目中所有的前端数据模拟（Mock）都必须通过 `vite-plugin-mock` 插件在 `src/mock/` 目录下进行拦截和处理。

**严禁**在 Vue 组件内部、`setup` 脚本中，或 `src/api/` 的函数中直接硬编码返回模拟数据（例如 `return Promise.resolve([...])`）。

为了确保 API 请求的可靠性和 Mock 拦截的稳定性，项目中所有动态的资源标识符（如 `projectId`, `documentId`, `userId` 等）**必须**作为 **Query Parameters** (查询参数) 传递。

**严禁**在 URL 路径中使用动态段（例如 `/api/project/:projectId`）。

-   **错误示范**: `url: '/api/project/:projectId/members'`
-   **正确示范**: `url: '/api/project/members'` (ID 通过 `?projectId=...` 传递)

### 实现流程

**第一步：在 `src/api/` 目录下定义 API 请求函数**

在调用 `get`, `post`, `put`, `del` 等请求函数时，将标识符作为参数传递。`axios` 会自动处理 `params` 对象为 URL 查询字符串。对于需要在 URL 中包含参数同时又需要请求体的 `POST` 或 `PUT` 请求，可以手动拼接 URL。

*   **文件位置**: `src/api/some-module.ts`
*   **示例**:

```typescript
// src/api/project.ts
import { get, post } from './request'
import type { ProjectMember } from '@/types/document'

/**
 * 正确：ID 通过 params 对象传递，axios 会将其转换为 ?projectId=...
 */
export function getProjectMembers(projectId: string) {
  return get<ProjectMember[]>('/api/project/members', { params: { projectId } });
}

/**
 * 正确：对于 POST/PUT 请求，可以将 ID 放在 URL 中，数据放在 body 中。
 */
export function saveProjectMembers(projectId: string, userIds: number[]) {
    return post(`/api/project/members/save?projectId=${projectId}`, { userIds });
}
```

**第二步：在 `src/mock/` 目录下配置 Mock 处理器**

Mock 配置中的 `url` 必须是**静态路径**，不包含任何动态 `:param` 片段。在 `response` 回调函数中，通过 `query` 对象来获取查询参数。

*   **文件位置**: `src/mock/some-module.ts`
*   **示例**:

```typescript
// src/mock/project.ts
import { defineMock } from 'vite-plugin-mock'
import type { ProjectMember } from '@/types/document'

const mockMembers: ProjectMember[] = [
  { userId: 49, identity: 'COLLABORATOR', userName: '测试', orgName: '景嘉微', picture: '...', position: '测试' },
  { userId: 46, identity: 'OWNER', userName: '向培', orgName: '研发管理部', picture: '', position: '开发' }
];

export default defineMock([
  {
    // 正确：URL 是静态的，不包含 ':'
    url: '/api/project/members',
    method: 'get',
    // 正确：通过 `query` 对象获取 projectId
    response: ({ query }) => {
      const { projectId } = query;
      console.log(`[Mock] Intercepted request for project members, ID: ${projectId}`);

      return {
        code: 200,
        msg: '操作成功',
        data: mockMembers
      }
    }
  },
  {
    url: '/api/project/members/save',
    method: 'post',
    response: ({ query, body }) => {
        const { projectId } = query;
        console.log(`[Mock] Saving members for project ID: ${projectId}`);
        console.log('[Mock] Received data:', body);
        return { code: 200, msg: '保存成功' };
    }
  }
])
```

### 总结要点

*   **URL 静态化**: `src/mock/` 中的 `url` 必须是静态字符串，以保证匹配的可靠性。
*   **参数来源**: `vite-plugin-mock` 会将 URL `?` 后面的所有参数解析到 `response` 回调的 `query` 对象中。
*   **职责分离**: `src/api/` 只负责定义"如何请求"，`src/mock/` 只负责定义"返回什么模拟数据"。
